import { LessonData } from '../index'

const w6: Record<number, LessonData> = {
  40: {
    id: 40,
    worldId: 6,
    title: "The Math Behind A Neuron",
    stops: [
      { tag: "Fact", title: "One Neuron Does One Thing", body: "A single artificial neuron takes a list of numbers as input, does one mathematical operation, and outputs one number. That's it. Everything in modern AI — ChatGPT, image recognition, AlphaGo — is built from billions of this same operation running simultaneously. Understanding one neuron completely is understanding the atomic unit of all of modern AI." },
      { tag: "Example", title: "The Weighted Sum", body: "Take inputs x₁, x₂, x₃. Each has a weight: w₁, w₂, w₃. The neuron multiplies each input by its weight and adds them together: (x₁ × w₁) + (x₂ × w₂) + (x₃ × w₃). Then adds a bias term b. The result is a single number. The weights determine how much each input influences the output. The bias shifts the output up or down regardless of the inputs. This weighted sum is the core computation of every neural network." },
      { tag: "Example", title: "Why Weights Matter", body: "If w₁ is large and positive, input x₁ strongly increases the output. If w₁ is large and negative, x₁ strongly decreases it. If w₁ is near zero, x₁ barely matters. The entire knowledge of a trained neural network is encoded in its weights. When you train a network you are adjusting millions or billions of these numbers until the network produces correct outputs. The weights are what the network learns." },
      { tag: "Fact", title: "The Bias Term", body: "The bias b is added after the weighted sum. It shifts the activation threshold — how much total input is needed before the neuron fires strongly. Without bias, every neuron's output would be zero when all inputs are zero, which limits what the network can represent. With bias, neurons can activate even when inputs are small, or stay inactive even when inputs are large. Bias gives each neuron flexibility to respond to patterns its weights alone couldn't capture." },
      { tag: "Example", title: "The Activation Function", body: "After computing the weighted sum plus bias, the result passes through an activation function. The simplest: if the result is positive output it, if negative output zero. This is ReLU — Rectified Linear Unit. Without activation functions, stacking layers of neurons would just be one big linear equation — which limits what the network can learn. Activation functions introduce nonlinearity — the ability to represent complex curved relationships rather than just straight lines." },
      { tag: "Hot take", title: "Why Nonlinearity Matters", body: "Most interesting patterns in the world are nonlinear. The relationship between pixel values and whether an image contains a face is not a straight line. The relationship between words in a sentence and their meaning is not a straight line. A network made only of linear operations can only learn linear relationships — which is almost useless for real problems. Activation functions are what make neural networks capable of learning the complex nonlinear patterns that make them powerful." },
      { tag: "Example", title: "A Neuron As A Decision Boundary", body: "Think of a single neuron as a classifier. It takes inputs, computes a weighted sum, and outputs a number that says how much the inputs match some pattern. A high output means \"yes this matches.\" A low output means \"no it doesn't.\" The weights define what pattern the neuron is looking for. Training adjusts those weights until the neuron reliably detects the pattern that helps produce correct outputs. Each neuron in a trained network is a tiny specialized detector." },
      { tag: "Big idea", title: "Composition", body: "The power of neural networks comes not from any single neuron but from composition — neurons feeding into neurons, each building on the previous layer's detections. One neuron detects a curve. Another detects that two curves are adjacent. Another detects that adjacent curves form an eye shape. Another detects that an eye shape and a nose shape together suggest a face. No single neuron does anything impressive. The composition of millions of simple detectors produces behavior that looks like understanding." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `A single artificial neuron takes multiple inputs, computes a weighted sum plus bias, and outputs a single number.`, answer: true, verdict: "Correct.", explanation: "This is the atomic operation of all modern AI. Every neuron in every layer does exactly this. Billions of these simple operations, organized into layers, produce the behavior we call artificial intelligence." },
      { difficulty: "Easy", tag: "Myth bust", stopTitle: '', question: `The weights in a neural network are set by the programmer before training and don't change.`, answer: false, verdict: "Wrong.", explanation: "Weights are initialized randomly and adjusted through training. The programmer defines the architecture. The specific weight values are learned from data through backpropagation. The knowledge of a trained network lives in its weights." },
      { difficulty: "Easy", tag: "Example", stopTitle: '', question: `Without activation functions, a deep neural network would be mathematically equivalent to a single linear transformation.`, answer: true, verdict: "Correct.", explanation: "Linear functions composed with linear functions produce linear functions. No matter how many linear layers you stack, you get one linear function. Activation functions introduce the nonlinearity that allows networks to approximate complex functions." },
      { difficulty: "Medium", tag: "Fact", stopTitle: '', question: `The bias term allows neurons to activate even when all inputs are zero.`, answer: true, verdict: "Correct.", explanation: "Without bias, a neuron with all-zero inputs would always output zero, limiting what the network can represent. Bias gives each neuron a baseline activation level independent of its inputs — necessary for networks to represent a wide range of functions." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `A neuron with a large positive weight for one input will be strongly inhibited by large values of that input.`, answer: false, verdict: "Wrong.", explanation: "The opposite. A large positive weight means that input strongly increases the neuron's output. A large negative weight would mean that input strongly decreases the output. The weight determines both the direction and magnitude of each input's influence." },
      { difficulty: "Medium", tag: "Big idea", stopTitle: '', question: `The composition of many simple neuron operations produces behavior that looks like understanding even though no single neuron understands anything.`, answer: true, verdict: "Correct.", explanation: "One neuron detects a curve. Another that two curves are adjacent. Another that adjacent curves form an eye. No individual neuron does anything impressive. The composition of millions of simple detectors produces recognition and behavior that looks like understanding." },
      { difficulty: "Hard", tag: "Fact", stopTitle: '', question: `All the knowledge in a trained neural network is stored in the weights and nothing else.`, answer: true, verdict: "Correct.", explanation: "The architecture defines the structure. The weights define the knowledge. Change the weights and you change what the network knows. Delete the weights and all learned knowledge is gone. This is why model weights are the valuable intellectual property of AI companies." },
      { difficulty: "Hard", tag: "Example", stopTitle: '', question: `A single neuron can be thought of as a tiny specialized detector that has learned to recognize a specific pattern in its inputs.`, answer: true, verdict: "Correct.", explanation: "Each neuron in a trained network has weights that make it respond strongly to certain input patterns and weakly to others. No individual detector is impressive. The power comes from millions of detectors organized hierarchically, each building on outputs of the previous layer." },
    ],
  },
  41: {
    id: 41,
    worldId: 6,
    title: "Forward Propagation",
    stops: [
      { tag: "Fact", title: "Data Flows Forward", body: "Forward propagation is what happens when you give a neural network an input and it produces an output. Data enters the first layer, gets transformed, passes to the next layer, gets transformed again, and continues until it reaches the output layer. Every transformation is the same operation — weighted sum plus bias plus activation function — applied by every neuron in every layer simultaneously. The forward pass is the network doing its job." },
      { tag: "Example", title: "The Input Layer", body: "The input layer doesn't compute anything — it just receives the raw data. For an image, each neuron in the input layer holds one pixel value. A 28×28 pixel image has 784 input neurons, one per pixel. For text, inputs are token embeddings — numerical representations of words. For audio, inputs are numerical representations of sound frequencies. The first transformation is always: turn whatever you want to process into a list of numbers." },
      { tag: "Example", title: "Hidden Layers", body: "Between input and output are hidden layers — called hidden because you don't directly observe their activations. Each hidden layer neuron receives outputs from every neuron in the previous layer, computes its weighted sum plus bias, applies its activation function, and passes its output to every neuron in the next layer. In a fully connected network every neuron connects to every neuron in adjacent layers. The connections are where the weights live." },
      { tag: "Fact", title: "What Activations Represent", body: "After forward propagation, each neuron in each layer has an activation value — the number it's outputting. In early layers these activations represent simple features — edges, colors, basic shapes. In middle layers they represent combinations of simple features — textures, object parts. In later layers they represent complex concepts — \"eye-like region,\" \"face-like structure,\" \"this is probably a dog.\" The activations become more abstract and more meaningful as data flows deeper." },
      { tag: "Hot take", title: "The Network Doesn't Know What It's Representing", body: "The neurons in a trained network don't know they're detecting eyes or faces. They don't have labels. They have weights that happen to produce useful activations for the task they were trained on. When we say \"this neuron detects edges\" we're describing what we observe about its behavior — not a property the neuron has. The network has no internal representation of what anything means. It has weights that produce correct outputs." },
      { tag: "Example", title: "The Output Layer", body: "The final layer produces the network's output. For image classification, the output layer has one neuron per possible class — one for cat, one for dog, one for bird. Each neuron outputs a number representing how likely the input is that class. The softmax activation function converts these numbers into probabilities that sum to 1. The class with the highest probability is the network's prediction. For language models, the output layer has one neuron per token in the vocabulary — usually tens of thousands." },
      { tag: "Scenario", title: "What Happens During A ChatGPT Response", body: "When you send ChatGPT a message, your text is converted to tokens, each token to a numerical embedding, these feed into the first transformer layer, each layer transforms the representations, after hundreds of layers the final layer outputs a probability distribution over every possible next token, the highest probability token is selected and added to the sequence, then the whole process repeats for the next token. Your entire conversation is reprocessed from scratch at each step. That's the forward pass — happening in milliseconds." },
      { tag: "Big idea", title: "Representation Learning", body: "The most important insight about forward propagation: what the network is really doing is learning representations. Raw inputs — pixels, tokens, audio samples — are not directly useful for making predictions. What's useful is a representation of those inputs that captures the features that matter for the task. Each layer transforms the representation into something more useful for the next layer. Deep networks are powerful because they learn rich hierarchical representations automatically from data. This is representation learning — the core of what makes neural networks work." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `Forward propagation is the process of data flowing from input to output through a neural network.`, answer: true, verdict: "Correct.", explanation: "Input data enters the first layer, gets transformed by each neuron, passes to the next layer, and continues until reaching the output. Every transformation is the weighted sum plus bias plus activation function. The forward pass is the network producing a prediction from an input." },
      { difficulty: "Easy", tag: "Myth bust", stopTitle: '', question: `The input layer of a neural network performs complex transformations on the raw data.`, answer: false, verdict: "Wrong.", explanation: "The input layer doesn't compute anything — it receives and holds the raw data. For images, each neuron holds one pixel value. The first actual computation happens in the first hidden layer. The input layer is an interface between raw data and the computational network." },
      { difficulty: "Easy", tag: "Scenario", stopTitle: '', question: `In a language model like ChatGPT, your entire conversation is reprocessed from scratch at each step when generating a response.`, answer: true, verdict: "Correct.", explanation: "Each new token requires a complete forward pass through the entire model using all previous tokens as context. The model has no persistent state between steps — all context must be in the input sequence processed by the forward pass." },
      { difficulty: "Medium", tag: "Myth bust", stopTitle: '', question: `Hidden layers are called 'hidden' because their activations are kept secret by the model's developers.`, answer: false, verdict: "Wrong.", explanation: "They're called hidden because you don't directly observe or control their activations — they're intermediate computations between input and output. Interpretability researchers do inspect them. The name refers to their position in the architecture, not any secrecy." },
      { difficulty: "Medium", tag: "Fact", stopTitle: '', question: `Activations in early layers of a deep network typically represent simple features, while later layers represent more complex and abstract features.`, answer: true, verdict: "Correct.", explanation: "Early layers detect edges and basic patterns. Middle layers combine these into shapes and textures. Later layers combine these into object parts and concepts. The abstraction increases with depth. This is why depth — not just width — matters for learning complex patterns." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `The softmax function used in output layers ensures that all output values sum to 1, forming a probability distribution.`, answer: true, verdict: "Correct.", explanation: "Softmax converts a vector of arbitrary numbers into probabilities between 0 and 1 that sum to 1. For classification, the output is a probability distribution over classes. For language models, it's a probability distribution over all possible next tokens — typically tens of thousands of options." },
      { difficulty: "Hard", tag: "Big idea", stopTitle: '', question: `Representation learning refers to the fact that neural networks learn to transform raw inputs into increasingly useful intermediate representations.`, answer: true, verdict: "Correct.", explanation: "Each layer transforms the representation into something more useful for subsequent layers. The network learns a hierarchy of representations that capture increasingly abstract and task-relevant features. This automatic representation learning is the core of what makes deep networks powerful." },
      { difficulty: "Hard", tag: "Hot take", stopTitle: '', question: `When researchers say 'this neuron detects edges,' they are describing a property the neuron itself has.`, answer: false, verdict: "Wrong.", explanation: "They're describing what they observe about the neuron's behavior — not a property the neuron has. The network has no internal representation of what anything means. It has weights that produce correct outputs. The interpretation is ours, not the network's." },
    ],
  },
  42: {
    id: 42,
    worldId: 6,
    title: "Loss Functions",
    stops: [
      { tag: "Fact", title: "What A Loss Function Is", body: "A loss function measures how wrong the network's output is. It takes the network's prediction and the correct answer and produces a single number — the loss — representing the error. High loss means the prediction was far from correct. Low loss means it was close. The entire training process is about minimizing this number. Without a loss function there's no signal to learn from — the network has no way to know how wrong it is." },
      { tag: "Example", title: "Mean Squared Error", body: "The simplest loss function: mean squared error. For a regression task — predicting a continuous value like house price — compute the difference between predicted and actual value, square it, average across all examples. Squaring serves two purposes: it makes all errors positive, and it penalizes large errors more than small ones. A prediction that's twice as far from the truth produces four times the loss. This pushes the network to avoid large mistakes." },
      { tag: "Example", title: "Cross-Entropy Loss", body: "For classification tasks — predicting which category something belongs to — cross-entropy loss is standard. It measures how surprised the network would be by the correct answer given its probability distribution. If the network predicted 90% probability for the correct class, the loss is low — not very surprised. If it predicted 5% probability for the correct class, the loss is high — very surprised. Cross-entropy penalizes confident wrong predictions more severely than uncertain wrong predictions." },
      { tag: "Hot take", title: "The Loss Function Defines What The Network Cares About", body: "Whatever you put in the loss function is what the network optimizes. Nothing else. If your loss function measures accuracy on a test set, the network optimizes test accuracy — which might involve memorizing rather than generalizing. If your loss function measures engagement, the network optimizes engagement — which might mean maximizing outrage. The loss function is the most important design decision in building an AI system. Every misalignment failure traces back to a loss function that didn't capture what you actually wanted." },
      { tag: "Fact", title: "The Loss Landscape", body: "Imagine a landscape where every point represents a possible set of weights and the height represents the loss for that set of weights. Training is the process of finding the lowest point in this landscape — the weights that produce minimum loss. For a network with billions of parameters, this landscape has billions of dimensions. Finding the minimum is not trivial. Gradient descent is the algorithm that navigates it — but the landscape has many local minima, saddle points, and flat regions that make navigation difficult." },
      { tag: "Example", title: "Regularization In The Loss", body: "Regularization adds a penalty to the loss function for large weights. The most common: L2 regularization adds the sum of squared weights to the loss. This encourages the network to use smaller weights — which tends to prevent overfitting by discouraging the network from relying too heavily on any single feature. The total loss becomes: task loss plus regularization penalty. The network now optimizes not just for correct predictions but for correct predictions with simple weights." },
      { tag: "Scenario", title: "Loss Functions In Language Models", body: "The loss function for training a language model like GPT is simple: predict the next token. Given a sequence of tokens, the model predicts what comes next. The loss is the cross-entropy between the model's predicted distribution and the actual next token. Do this for every position in every sequence in the training data — hundreds of billions of predictions. The model that minimizes this loss has learned, as a side effect, an enormous amount about language, facts, reasoning, and the world. All from predicting the next word." },
      { tag: "Big idea", title: "Loss As Specification", body: "The loss function is the clearest example of the alignment problem in concrete technical form. You cannot optimize for something you can't measure. If you can measure it precisely you can optimize for it. But the things you actually care about — helpful responses, fair decisions, beneficial outcomes — are often hard to measure precisely. So you proxy them with things that are measurable — task accuracy, engagement, user ratings. The proxy is never perfect. The gap between the proxy and the real objective is where misalignment lives." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `A loss function measures how wrong the network's output is compared to the correct answer.`, answer: true, verdict: "Correct.", explanation: "It takes the prediction and the correct answer and produces a single number — the loss. High loss means far from correct, low loss means close. Training is the process of adjusting weights to minimize this number. Without a loss function there's no signal to learn from." },
      { difficulty: "Easy", tag: "Myth bust", stopTitle: '', question: `Mean squared error is commonly used for classification tasks like identifying whether an image contains a cat.`, answer: false, verdict: "Wrong.", explanation: "Mean squared error is for regression — predicting continuous values like house prices. For classification, cross-entropy loss is standard. It measures how surprised the network would be by the correct answer given its predicted probabilities." },
      { difficulty: "Easy", tag: "Hot take", stopTitle: '', question: `The loss function is one of the most important design decisions in building an AI system because it defines exactly what the network optimizes for.`, answer: true, verdict: "Correct.", explanation: "Whatever you put in the loss function is what the network cares about — nothing else. Every misalignment failure traces back to a loss function that didn't capture what was actually wanted. The specification of the loss function is the specification of the system's objective." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `Cross-entropy loss penalizes uncertain wrong predictions more severely than confident wrong predictions.`, answer: false, verdict: "Wrong.", explanation: "The opposite. If the network predicted 90% for the wrong class — very confident and wrong — the loss is high. If it predicted 15% for the correct class — uncertain — the loss is lower. Cross-entropy encourages calibration: be confident when right, uncertain when uncertain." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `Regularization adds a penalty to the loss function that encourages the network to learn simpler solutions with smaller weights.`, answer: true, verdict: "Correct.", explanation: "L2 regularization adds the sum of squared weights to the loss. This prevents overfitting by stopping the network from relying too heavily on any single feature. The network optimizes for correct predictions AND simple weights simultaneously." },
      { difficulty: "Medium", tag: "Fact", stopTitle: '', question: `The loss landscape for a network with billions of parameters has billions of dimensions.`, answer: true, verdict: "Correct.", explanation: "Every parameter is a dimension. Finding the minimum of a function in billions of dimensions is not trivial. The landscape has many local minima, saddle points, and flat regions. Much of the art of training involves managing gradient descent's navigation of this landscape." },
      { difficulty: "Hard", tag: "Scenario", stopTitle: '', question: `The training objective for GPT — predict the next token — is a simple loss function that produces complex capabilities as a side effect.`, answer: true, verdict: "Correct.", explanation: "Cross-entropy between predicted and actual next tokens, applied to hundreds of billions of examples. A model that minimizes this loss has learned an enormous amount about language, facts, reasoning, and the world. The simplicity of the objective and the complexity of what it produces is one of the most remarkable aspects of large language model training." },
      { difficulty: "Hard", tag: "Big idea", stopTitle: '', question: `The gap between a measurable proxy loss and the actual objective you care about is where misalignment in AI systems originates.`, answer: true, verdict: "Correct.", explanation: "You can only optimize for what you can measure. The things you actually care about are hard to measure precisely, so you proxy them with measurable quantities. The proxy is never perfect. That gap is exactly where reward hacking, sycophancy, and other alignment failures live." },
    ],
  },
  43: {
    id: 43,
    worldId: 6,
    title: "Backpropagation In Detail",
    stops: [
      { tag: "Fact", title: "The Learning Algorithm", body: "Backpropagation is the algorithm that trains neural networks. It computes how much each weight contributed to the loss and adjusts each weight in the direction that reduces the loss. Without backpropagation — or something equivalent — there's no efficient way to train deep networks. It was known theoretically in the 1970s, demonstrated in the 1980s, and scaled to transform the field in 2012. Most of modern AI runs on this algorithm." },
      { tag: "Example", title: "Gradient Descent", body: "Backpropagation computes gradients — the direction and magnitude of how the loss changes with respect to each weight. Gradient descent uses these to update weights: new weight = old weight minus learning rate times gradient. The learning rate controls how big each update step is. Too large and training overshoots. Too small and training is slow. The gradient tells you which direction to move. The learning rate tells you how far." },
      { tag: "Example", title: "The Chain Rule", body: "Backpropagation uses calculus — specifically the chain rule. To know how much weight w in layer 1 contributed to the final loss, you need to trace the effect of w through every subsequent layer to the output. The chain rule lets you compute this by multiplying the local gradient at each step. The gradient of the loss with respect to w equals the gradient of the loss with respect to the next layer's output, times the gradient of that output with respect to w. Repeat backwards through every layer." },
      { tag: "Hot take", title: "Why Backpropagation Was Controversial", body: "In the 1980s many researchers doubted backpropagation would work for deep networks. The vanishing gradient problem: as gradients are multiplied through many layers, they tend to shrink toward zero in early layers — those layers stop learning. This limited networks to shallow architectures for decades. The problem was solved through better activation functions, better weight initialization, residual connections, and batch normalization. Modern deep learning is largely a story of solving the problems that once made deep networks untrainable." },
      { tag: "Fact", title: "Stochastic Gradient Descent", body: "Computing the gradient over the entire training dataset before each update is expensive. Stochastic gradient descent computes the gradient on a single randomly selected example instead. This is noisy but fast. Mini-batch gradient descent — the most common approach — computes the gradient on a small batch of examples, typically 32 to 512. Batches provide a balance between the accuracy of full-dataset gradients and the speed of single-example gradients." },
      { tag: "Example", title: "Optimizers", body: "Plain gradient descent often trains slowly or gets stuck. Optimizers improve on it. Momentum accumulates past gradients to build speed in consistent directions — like a ball rolling downhill. Adam combines momentum with adaptive learning rates — larger steps for weights that haven't changed much, smaller steps for weights that change frequently. Most large models are trained with Adam or its variants. The choice of optimizer significantly affects training speed and final performance." },
      { tag: "Scenario", title: "What Happens In One Training Step", body: "One complete training step: sample a batch of examples from the training data. Run forward propagation — compute activations for every neuron in every layer. Compute the loss — how wrong was the output? Run backpropagation — compute the gradient of the loss with respect to every weight in the network. Update every weight using the optimizer. For a large language model this involves computing gradients for hundreds of billions of parameters. Do this millions of times. That's training." },
      { tag: "Big idea", title: "Credit Assignment", body: "The fundamental problem backpropagation solves is credit assignment — given that the network made a wrong prediction, which of the billions of weights are responsible and by how much? Each weight influenced the output through a complex chain of computations involving every subsequent weight. The chain rule lets you assign credit precisely — compute exactly how much each weight contributed to the error. This precise credit assignment is what makes gradient-based learning work. Without it, training would require random search — trying random weight updates and keeping ones that help. Backpropagation makes training millions of times more efficient." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `Backpropagation computes how much each weight contributed to the loss and adjusts weights to reduce that loss.`, answer: true, verdict: "Correct.", explanation: "It solves the credit assignment problem using the chain rule of calculus. It computes gradients for every weight in the network and uses these to update weights in the direction that reduces loss. This is the algorithm that makes training neural networks possible." },
      { difficulty: "Easy", tag: "Example", stopTitle: '', question: `The learning rate controls how large each weight update step is during training.`, answer: true, verdict: "Correct.", explanation: "New weight = old weight minus learning rate times gradient. Too large a learning rate and training overshoots good solutions. Too small and training is slow. Modern optimizers adapt the learning rate per parameter, but the basic principle holds." },
      { difficulty: "Easy", tag: "Myth bust", stopTitle: '', question: `Stochastic gradient descent computes gradients on the entire training dataset before each weight update.`, answer: false, verdict: "Wrong.", explanation: "That would be full-batch gradient descent. Stochastic gradient descent uses a single randomly selected example per update. Mini-batch gradient descent — the most common approach — uses small batches, balancing accuracy of full-dataset gradients with computational efficiency." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `The chain rule is used in backpropagation to compute how a weight in an early layer contributed to the final loss through all subsequent layers.`, answer: true, verdict: "Correct.", explanation: "The effect of a weight in layer 1 passes through every subsequent layer. The chain rule computes this by multiplying local gradients at each step. This makes gradient computation tractable in deep networks — without it you'd need to numerically estimate gradients for each weight separately." },
      { difficulty: "Medium", tag: "Hot take", stopTitle: '', question: `The vanishing gradient problem caused gradients to grow very large in early layers of deep networks, making training unstable.`, answer: false, verdict: "Wrong.", explanation: "The vanishing gradient problem causes gradients to shrink — approach zero — in early layers. As gradients are multiplied through many layers they get smaller. Near-zero gradients mean those layers stop learning. This limited networks to shallow architectures for decades." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `Momentum in optimization accumulates past gradients to build speed in consistent directions, similar to a ball rolling downhill.`, answer: true, verdict: "Correct.", explanation: "Rather than updating based only on the current gradient, momentum maintains a running average of past gradients. In directions where gradients consistently point the same way, momentum builds speed. In directions where they oscillate, it dampens them. This produces faster convergence than plain gradient descent." },
      { difficulty: "Hard", tag: "Example", stopTitle: '', question: `The Adam optimizer adapts learning rates per parameter — larger steps for parameters that haven't changed much, smaller steps for those that change frequently.`, answer: true, verdict: "Correct.", explanation: "Adam combines momentum with adaptive per-parameter learning rates. Parameters with consistently large gradients get smaller learning rates. Parameters with consistently small gradients get larger learning rates. This makes Adam robust across different types of parameters and loss landscapes." },
      { difficulty: "Hard", tag: "Big idea", stopTitle: '', question: `Without backpropagation, training neural networks would require random search — trying random weight updates and keeping ones that help — which would be millions of times less efficient.`, answer: true, verdict: "Correct.", explanation: "Backpropagation's key contribution is efficient credit assignment. For a network with billions of parameters, computing gradients analytically rather than through random search makes training tractable. Without it, modern large-scale neural networks would be computationally impossible." },
    ],
  },
  44: {
    id: 44,
    worldId: 6,
    title: "Activation Functions",
    stops: [
      { tag: "Fact", title: "Why Activation Functions Exist", body: "Without activation functions, a neural network — no matter how many layers — is equivalent to a single linear transformation. Stack as many linear layers as you want, you still get a linear function. Linear functions can only learn linear relationships. The real world is full of nonlinear relationships. Activation functions introduce nonlinearity — the mathematical property that allows neural networks to approximate any function, including the complex ones that describe real patterns in data." },
      { tag: "Example", title: "Sigmoid", body: "The sigmoid function maps any input to a value between 0 and 1. Output = 1 / (1 + e^(-x)). Historically important — used in early neural networks and still used in output layers for binary classification. Problem: for very large or very small inputs the sigmoid saturates — its gradient approaches zero. During backpropagation, near-zero gradients multiplied through many layers produce vanishing gradients. This is why sigmoid fell out of favor for hidden layers in deep networks." },
      { tag: "Example", title: "ReLU", body: "Rectified Linear Unit: output = max(0, x). If input is positive, output equals input. If input is negative, output is zero. Simple, fast, and effective. ReLU doesn't saturate for positive inputs — gradients flow cleanly through layers. It introduced a massive practical improvement in training deep networks when it became standard around 2012. One problem: neurons with negative inputs output zero and have zero gradient — the dying ReLU problem. Variants like Leaky ReLU address this by allowing a small gradient for negative inputs." },
      { tag: "Example", title: "Softmax", body: "Softmax is used in output layers for multiclass classification. It takes a vector of numbers and converts them to a probability distribution — all values between 0 and 1, summing to 1. The largest input gets the largest probability. Softmax is differentiable, which means gradients flow through it during backpropagation. It's used in the final layer of image classifiers and in the attention mechanism of transformers. You've encountered its output every time a language model assigns probabilities to possible next tokens." },
      { tag: "Fact", title: "GELU and Modern Activations", body: "Gaussian Error Linear Unit — GELU — is used in most modern large language models including GPT. Unlike ReLU it doesn't have a hard cutoff at zero — it applies a smooth approximation that allows small gradients for negative inputs. GELU tends to produce better performance than ReLU on language tasks. The exact reasons are not fully understood. In practice, different activation functions work better for different architectures and tasks, and the best choice is often determined empirically." },
      { tag: "Hot take", title: "The Activation Function Is A Design Choice", body: "The choice of activation function is a hyperparameter — a design choice made before training that affects what the network can learn and how easily it trains. These choices are not derived from first principles. They are determined by experimentation, accumulated intuition, and sometimes mathematical analysis of properties like gradient flow. The history of neural networks includes multiple activation function revolutions — each enabling deeper networks or better performance. This is typical of the field: empirical discoveries rather than deductive proofs." },
      { tag: "Example", title: "Activation Functions In Transformers", body: "Modern transformers use different activation functions in different places. Attention uses softmax to produce probability distributions over positions. Feed-forward layers use GELU or similar smooth nonlinearities. Output layers use softmax again for token predictions. The choice of different activations for different components reflects their different mathematical roles — probability distributions need softmax, hidden representations benefit from smooth nonlinearities, gating mechanisms benefit from sigmoid. Each component is designed for its specific function." },
      { tag: "Big idea", title: "Inductive Biases", body: "Every design choice in a neural network — including activation function — encodes an inductive bias: an assumption about what kind of solution is being sought. ReLU assumes that negative activations should be suppressed. Softmax assumes outputs should be a probability distribution. These assumptions are not neutral — they shape what the network can learn and how it learns it. Understanding neural networks means understanding the inductive biases encoded in their design choices, because those biases determine what patterns the network is capable of finding." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `Activation functions are used to introduce nonlinearity into neural networks.`, answer: true, verdict: "Correct.", explanation: "Without them, stacking linear layers produces a linear function regardless of depth. Linear functions can only represent linear relationships. The real world is nonlinear. Activation functions allow networks to approximate any function — including complex nonlinear ones." },
      { difficulty: "Easy", tag: "Example", stopTitle: '', question: `ReLU outputs zero for negative inputs and passes positive inputs through unchanged.`, answer: true, verdict: "Correct.", explanation: "ReLU: output = max(0, x). Simple, fast, and effective. It doesn't saturate for positive inputs — gradients flow cleanly. It became standard around 2012 and was a significant improvement over sigmoid for deep networks." },
      { difficulty: "Easy", tag: "Myth bust", stopTitle: '', question: `The sigmoid activation function is ideal for hidden layers in deep networks because it doesn't cause vanishing gradients.`, answer: false, verdict: "Wrong.", explanation: "Sigmoid saturates for large or small inputs — its gradient approaches zero. During backpropagation, near-zero gradients multiplied through many layers produce vanishing gradients. This is why sigmoid fell out of favor for hidden layers and was replaced by ReLU and variants." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `Softmax converts a vector of numbers into a probability distribution where all values sum to 1.`, answer: true, verdict: "Correct.", explanation: "Used in output layers for classification and in the attention mechanism. For any input vector, softmax produces positive values between 0 and 1 that sum to 1. It's differentiable, so gradients flow through it. You encounter its output every time a language model assigns probabilities to possible next tokens." },
      { difficulty: "Medium", tag: "Myth bust", stopTitle: '', question: `GELU is used in most modern large language models because it is mathematically simpler than ReLU.`, answer: false, verdict: "Wrong.", explanation: "GELU is actually more mathematically complex than ReLU. It applies a smooth approximation allowing small gradients for negative inputs rather than a hard cutoff. It tends to produce better performance on language tasks. The choice is largely empirical — simplicity is not why it's used." },
      { difficulty: "Medium", tag: "Big idea", stopTitle: '', question: `Every design choice in a neural network, including activation function, encodes an inductive bias — an assumption about what kind of solution is being sought.`, answer: true, verdict: "Correct.", explanation: "ReLU assumes negative activations should be suppressed. Softmax assumes outputs should be a probability distribution. These assumptions shape what the network can learn. Understanding neural networks means understanding these inductive biases — they determine what patterns the network can find." },
      { difficulty: "Hard", tag: "Example", stopTitle: '', question: `Different activation functions are used in different components of modern transformers because different components have different mathematical requirements.`, answer: true, verdict: "Correct.", explanation: "Attention uses softmax for probability distributions over positions. Feed-forward layers use GELU for hidden representations. Output layers use softmax for token predictions. Each choice reflects the mathematical role of that component." },
      { difficulty: "Hard", tag: "Hot take", stopTitle: '', question: `The choice of activation function is derived from first principles and can be determined theoretically before any experiments.`, answer: false, verdict: "Wrong.", explanation: "Activation function choices are largely empirical — determined through experimentation, accumulated intuition, and sometimes mathematical analysis. GELU outperforms ReLU on language tasks for reasons not fully understood theoretically. This is typical of the field: empirical discoveries rather than deductive proofs." },
    ],
  },
  45: {
    id: 45,
    worldId: 6,
    title: "Attention Mechanisms",
    stops: [
      { tag: "Fact", title: "The Attention Revolution", body: "The attention mechanism, introduced in a 2017 paper titled \"Attention Is All You Need,\" fundamentally changed AI. Before attention, networks processed sequences step by step and struggled to connect information from far apart in a sequence. Attention allows every position in a sequence to directly attend to every other position — computing how relevant each position is to every other. This single innovation enabled transformers, which enabled GPT, which enabled the current AI moment." },
      { tag: "Example", title: "The Intuition", body: "Imagine reading: \"The animal didn't cross the street because it was too tired.\" What does \"it\" refer to? You instantly know — the animal, not the street. You did this by attending to \"animal\" and \"tired\" and recognizing their connection. Attention in neural networks does something analogous — for each word in a sequence, it computes a weighted sum of all other words' representations, with higher weights for more relevant words. \"It\" attends strongly to \"animal\" because that connection produces useful representations." },
      { tag: "Example", title: "Query, Key, Value", body: "Attention uses three learned matrices to project each token's representation into three spaces: Query (Q), Key (K), and Value (V). For each position, the query represents \"what am I looking for?\" Keys represent \"what does each position have to offer?\" Values represent \"what information does each position contain?\" Attention scores are computed as the dot product of queries and keys — measuring relevance. These scores are softmaxed to produce weights, which are applied to values. The output is a weighted sum of all values, weighted by relevance." },
      { tag: "Fact", title: "Scaled Dot-Product Attention", body: "The full formula: Attention(Q, K, V) = softmax(QK^T / √d_k)V. The division by √d_k — the square root of the key dimension — scales the dot products to prevent them from getting very large when dimensions are high. Large dot products push softmax into regions with very small gradients, making training slow. Scaling keeps gradients healthy. This is one of many implementation details that separate a theoretical idea from a working system." },
      { tag: "Example", title: "Multi-Head Attention", body: "Rather than computing attention once, transformers compute it multiple times in parallel — each \"head\" with different Q, K, V matrices. Each head can attend to different aspects of the sequence. One head might capture syntactic relationships. Another might capture semantic relationships. Another might track coreference — what \"it\" refers to. The outputs of all heads are concatenated and linearly projected. Multi-head attention allows the network to simultaneously represent multiple types of relationships between positions." },
      { tag: "Hot take", title: "Attention Is Expensive", body: "Computing attention requires comparing every position to every other position — a quadratic complexity in sequence length. For a sequence of 1000 tokens, that's 1,000,000 comparisons per layer. For sequences of tens of thousands of tokens, attention becomes extremely expensive. This is one of the main limitations of transformer architectures — processing very long documents or maintaining long context requires significant computational resources. Much research is devoted to making attention more efficient." },
      { tag: "Scenario", title: "What Attention Does In GPT", body: "When GPT generates a response, each layer of the transformer applies multi-head attention to the entire sequence so far. Each token's representation is updated based on how it attends to all previous tokens. Later layers build on earlier ones — early layers capture local syntax, later layers capture long-range semantic relationships. By the final layer, each position's representation has been shaped by every other position in the context. The probability distribution over the next token is computed from this richly contextualized representation." },
      { tag: "Big idea", title: "Context As Computation", body: "The key insight of attention is that meaning is contextual — the representation of any part of a sequence should be informed by its relationship to every other part. A word doesn't mean the same thing in isolation and in context. A pixel doesn't have the same significance alone and in relation to surrounding pixels. Attention is a mechanism for computing context-dependent representations — allowing each element to be interpreted in light of everything else. This is why transformers are powerful for language, vision, and any domain where context matters. Which is almost every domain." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `The attention mechanism allows every position in a sequence to directly attend to every other position.`, answer: true, verdict: "Correct.", explanation: "This is the key innovation. Before attention, networks processed sequences step by step and struggled to connect distant information. Attention computes, for each position, a weighted combination of all other positions based on relevance. This enabled transformers and the current AI moment." },
      { difficulty: "Easy", tag: "Example", stopTitle: '', question: `Attention in neural networks was inspired by how humans selectively focus on relevant parts of a sequence.`, answer: true, verdict: "Correct.", explanation: "The intuition from human cognition motivated the mechanism. When reading \"The animal didn't cross the street because it was too tired,\" you attend to \"animal\" when resolving \"it.\" The neural network mechanism formalizes this: for each position, compute relevance to every other position and weight their representations accordingly." },
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `The scaled dot-product attention formula divides by the square root of the key dimension to prevent gradients from becoming very small.`, answer: true, verdict: "Correct.", explanation: "Large dot products push softmax into regions with very small gradients, making training slow. Scaling by √d_k keeps dot products in a range where gradients remain healthy. This is one of many implementation details that separate a working system from a theoretical idea." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `In the Query-Key-Value attention framework, the query represents what a position is looking for, and the key represents what each position offers.`, answer: true, verdict: "Correct.", explanation: "For each position, its query is compared to all other positions' keys by dot product — measuring relevance. The softmaxed attention weights are applied to values. The output is a weighted sum of values, weighted by how relevant each position's key was to the current position's query." },
      { difficulty: "Medium", tag: "Myth bust", stopTitle: '', question: `Multi-head attention computes attention only once but with a very large matrix to capture all types of relationships.`, answer: false, verdict: "Wrong.", explanation: "Multi-head attention computes attention multiple times in parallel — each head with different Q, K, V matrices. Each head can capture different relationship types. Computing attention once with a larger matrix is not equivalent — different heads genuinely learn to attend to different aspects of the sequence." },
      { difficulty: "Medium", tag: "Hot take", stopTitle: '', question: `Attention computation has quadratic complexity in sequence length, making it expensive for very long sequences.`, answer: true, verdict: "Correct.", explanation: "Every position attends to every other — for N tokens, that's N² comparisons per layer. For 1000 tokens: 1,000,000. For 100,000 tokens: 10 billion. This is one of the main limitations of transformers and a major research focus." },
      { difficulty: "Hard", tag: "Big idea", stopTitle: '', question: `The key insight of attention is that meaning is contextual — the representation of any element should be informed by its relationship to every other element.`, answer: true, verdict: "Correct.", explanation: "A word doesn't mean the same in isolation and in context. Attention computes context-dependent representations — allowing each element to be interpreted in light of everything else. This is why transformers work for language, vision, and any domain where context matters." },
      { difficulty: "Hard", tag: "Myth bust", stopTitle: '', question: `Attention is a complete solution to the problem of long-range dependencies in sequences.`, answer: false, verdict: "Wrong.", explanation: "Attention enables long-range dependencies in principle but the quadratic cost limits practical context length. Attention weights also don't guarantee that distant information is correctly integrated. Attention is necessary but not sufficient — depth, feed-forward capacity, and training all matter too." },
    ],
  },
  46: {
    id: 46,
    worldId: 6,
    title: "The Transformer Architecture",
    stops: [
      { tag: "Fact", title: "The Architecture That Changed Everything", body: "The transformer architecture, introduced in 2017, is the foundation of almost every major AI system built since then. GPT, BERT, T5, PaLM, Claude, Gemini — all transformers. AlphaFold — transformer. The image models underlying DALL-E — transformer. The architecture is not the only approach to AI, but it is currently dominant across almost every domain where AI has achieved remarkable performance." },
      { tag: "Example", title: "The Encoder-Decoder Structure", body: "The original transformer had two components: an encoder that processes the input sequence and builds representations, and a decoder that generates the output sequence one token at a time while attending to the encoder's representations. This structure is natural for translation — encode the source language, decode into the target language. GPT-style models use only the decoder — generating text by predicting the next token. BERT-style models use only the encoder — building rich representations for classification tasks." },
      { tag: "Fact", title: "Positional Encoding", body: "Attention has no inherent sense of order — it treats every position symmetrically. \"The cat sat\" and \"sat cat The\" would produce the same attention scores without positional information. Positional encoding adds order information to token embeddings before they enter the transformer. The original paper used sine and cosine functions of different frequencies. Modern models often learn positional encodings directly. Without positional encoding, transformers can't distinguish between different orderings of the same tokens." },
      { tag: "Example", title: "The Transformer Layer", body: "Each transformer layer has two sublayers. The first is multi-head self-attention — each token attends to all other tokens and updates its representation. The second is a feed-forward network — two linear transformations with a nonlinear activation applied independently to each position. Both sublayers use residual connections — adding the input to the output — and layer normalization. The residual connections are what make training very deep transformers possible by preventing vanishing gradients." },
      { tag: "Hot take", title: "The Feed-Forward Layer Is Underappreciated", body: "Most discussion of transformers focuses on attention. The feed-forward layers — which constitute about two-thirds of a transformer's parameters — are less discussed but equally important. Research suggests they act as a kind of associative memory — storing factual associations learned during training. When a language model knows that Paris is the capital of France, that knowledge is likely stored in the feed-forward layers, not the attention mechanism. The transformer's knowledge is distributed between its attention patterns and its feed-forward memories." },
      { tag: "Fact", title: "Scale And Emergence", body: "Large transformers exhibit emergent capabilities — abilities that appear suddenly at scale that weren't present in smaller models. Models below a certain size fail completely at some tasks. Above a threshold, performance jumps. Chain-of-thought reasoning, arithmetic, in-context learning, instruction following — all emerged at scale. These emergent capabilities were not designed in and were not predicted. They appear to be properties of very large learned representations that we don't fully understand. Emergence is one of the most surprising and least understood aspects of large transformers." },
      { tag: "Scenario", title: "Why GPT-4 Is Different From GPT-2", body: "GPT-2 (2019) had 1.5 billion parameters. GPT-4 (2023) is estimated at over 1 trillion. The architecture is similar. The scale is different by three orders of magnitude. Between those two models: the ability to follow complex instructions, to reason through multi-step problems, to write code, to understand nuance, to maintain coherent long conversations, to learn from examples in context. These capabilities did not exist at GPT-2 scale. Scale didn't just improve existing capabilities — it created new ones. This is why the trajectory of transformer scaling matters so much." },
      { tag: "Big idea", title: "Architecture As Hypothesis", body: "The transformer architecture is a hypothesis about what structure is useful for learning from data. The hypothesis is: sequences of tokens, processed by alternating attention and feed-forward layers, with residual connections and layer normalization, at sufficient scale. This hypothesis has been validated empirically across an enormous range of tasks and domains. But it's not the only possible hypothesis. Researchers are exploring alternatives — state space models, mixture of experts, diffusion models for language. The current dominance of transformers reflects empirical success, not theoretical inevitability." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `The transformer architecture is the foundation of most major AI systems built since 2017.`, answer: true, verdict: "Correct.", explanation: "GPT, BERT, T5, PaLM, Claude, Gemini — all transformers. AlphaFold — transformer. Image generation models — transformer-based. The architecture is dominant across language, vision, protein structure prediction, and many other domains." },
      { difficulty: "Easy", tag: "Myth bust", stopTitle: '', question: `GPT-style models use only the encoder component of the original transformer architecture.`, answer: false, verdict: "Wrong.", explanation: "GPT-style models use only the decoder. The encoder builds representations. The decoder generates output sequences token by token. GPT generates text by predicting the next token autoregressively — a decoder operation. BERT, which builds representations for classification, uses only the encoder." },
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `Without positional encoding, a transformer would treat the same words in different orders identically.`, answer: true, verdict: "Correct.", explanation: "Attention treats all positions symmetrically. Without positional information, 'the cat sat' and 'sat cat the' would produce the same attention scores. Positional encoding adds order information, allowing the model to distinguish positions and represent the sequential structure fundamental to language." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `Residual connections in transformer layers add the layer's input to its output, which helps prevent vanishing gradients in deep networks.`, answer: true, verdict: "Correct.", explanation: "Rather than just passing the transformed representation forward, residual connections add the original input: output = layer(input) + input. This provides a gradient highway through the network. Residual connections are what make training very deep transformers stable." },
      { difficulty: "Medium", tag: "Myth bust", stopTitle: '', question: `The feed-forward layers in transformers are less important than attention layers and could be removed without significant performance loss.`, answer: false, verdict: "Wrong.", explanation: "Feed-forward layers constitute about two-thirds of a transformer's parameters and are essential. Research suggests they act as associative memory — storing factual knowledge. Removing them would dramatically reduce capacity to store and retrieve information." },
      { difficulty: "Medium", tag: "Myth bust", stopTitle: '', question: `Emergent capabilities in large language models are capabilities that were explicitly programmed in as the models scaled up.`, answer: false, verdict: "Wrong.", explanation: "Emergent capabilities appear at scale without being designed in and without being predicted. Chain-of-thought reasoning, arithmetic, in-context learning — all emerged in larger models without being explicitly trained for. Emergence is discovered empirically." },
      { difficulty: "Hard", tag: "Big idea", stopTitle: '', question: `The transformer architecture is a hypothesis about what structure is useful for learning from data — validated empirically but not the only possible hypothesis.`, answer: true, verdict: "Correct.", explanation: "Researchers are exploring alternatives — state space models, mixture of experts, diffusion models for language. The current dominance of transformers reflects empirical success across many tasks, not theoretical inevitability. Architecture is a design choice that encodes assumptions about useful structure." },
      { difficulty: "Hard", tag: "Myth bust", stopTitle: '', question: `The capability gap between GPT-2 and GPT-4 demonstrates that scaling produces only quantitative improvements in existing capabilities, not qualitative new ones.`, answer: false, verdict: "Wrong.", explanation: "This is exactly what emergence contradicts. The ability to follow complex instructions, reason through multi-step problems, write code — these did not exist at GPT-2 scale. Scale didn't just improve existing capabilities. It created new ones. This qualitative change is what makes transformer scaling so significant." },
    ],
  },
  47: {
    id: 47,
    worldId: 6,
    title: "Training At Scale",
    stops: [
      { tag: "Fact", title: "What Training A Large Model Actually Involves", body: "Training GPT-4 required thousands of specialized AI processors running continuously for months. The training data was hundreds of billions of words. The model has hundreds of billions of parameters. Each training step involves a forward pass computing activations for every parameter, a loss computation, a backward pass computing gradients for every parameter, and a parameter update. Do this billions of times. The compute cost is estimated in the hundreds of millions of dollars. Training at scale is an industrial process." },
      { tag: "Example", title: "Learning Rate Schedules", body: "The learning rate — how much to adjust weights per step — doesn't stay constant during training. Learning rate schedules change it systematically. A common approach: warm up — start with a very small learning rate and increase it over the first few thousand steps, allowing the model to find a good region before taking large steps. Then decay — gradually decrease the learning rate as training progresses, taking smaller steps as the model converges. The schedule significantly affects final performance and training stability." },
      { tag: "Example", title: "Batch Normalization And Layer Normalization", body: "Deep networks are hard to train partly because the distribution of activations changes as weights change — early layers keep shifting the input distribution that later layers see. Normalization layers address this by standardizing activations within each batch or each layer. Layer normalization — used in transformers — normalizes across the features of each individual example. This stabilizes training and allows much higher learning rates. Without normalization, training very deep networks tends to be unstable." },
      { tag: "Fact", title: "Dropout", body: "Dropout randomly sets a fraction of neurons' activations to zero during training. This prevents neurons from co-adapting — becoming dependent on specific other neurons. It forces the network to learn redundant representations — multiple ways to represent the same information — which improves generalization. Dropout is not used during inference — only during training. It acts as a form of ensemble learning: training many different subnetworks simultaneously and averaging their predictions." },
      { tag: "Hot take", title: "Data Quality Matters More Than Quantity", body: "The conventional wisdom was that more data always helps. Recent research has complicated this. Training on filtered, high-quality data often outperforms training on more data of lower quality. The Chinchilla scaling laws (2022) showed that most large models at the time were undertrained on too little data relative to their size — more tokens per parameter would have been more efficient. Data curation — deciding what to train on and what to exclude — is one of the most important and least discussed aspects of training large models." },
      { tag: "Example", title: "Distributed Training", body: "No single processor can hold the parameters of a 100-billion parameter model in memory. Distributed training splits the model across many processors. Model parallelism splits the model itself — different layers or components run on different devices. Data parallelism runs copies of the model on different devices, each processing different data, with gradients synchronized. Pipeline parallelism chains stages of the forward and backward pass. Modern large model training uses combinations of all three. Coordinating billions of operations across thousands of processors is an engineering challenge as much as a research one." },
      { tag: "Scenario", title: "What Happens After Pretraining", body: "Pretraining produces a model that predicts text — but not necessarily one that follows instructions helpfully. After pretraining, large language models typically undergo fine-tuning. Supervised fine-tuning on examples of desired behavior. Then RLHF — reinforcement learning from human feedback — trains the model to produce outputs humans rate as helpful, harmless, and honest. The pretrained model provides broad capabilities. Fine-tuning and RLHF shape how those capabilities are applied. ChatGPT and Claude are not just pretrained models — they are pretrained models shaped by significant post-training." },
      { tag: "Big idea", title: "Engineering At The Frontier", body: "Training large AI models is frontier engineering — working at the edge of what hardware, software, and mathematical understanding make possible. Every component matters: the architecture, the data, the loss function, the optimizer, the learning rate schedule, the normalization strategy, the regularization. A mistake in any of these can make training unstable, slow convergence, or produce a model that performs poorly. The researchers who build frontier models are not just applying known techniques — they are discovering what works through careful experimentation at enormous scale. Modern AI is simultaneously a scientific discipline, an engineering practice, and an empirical art." },
    ],
    questions: [
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `Training a large language model like GPT-4 requires thousands of specialized processors running for months at a cost of hundreds of millions of dollars.`, answer: true, verdict: "Correct.", explanation: "Training at scale is an industrial process. This is why only a handful of organizations can train frontier models. The resources required create the concentration in AI development that has significant implications for governance and who shapes the trajectory of AI." },
      { difficulty: "Easy", tag: "Myth bust", stopTitle: '', question: `The learning rate stays constant throughout training for best results.`, answer: false, verdict: "Wrong.", explanation: "Learning rate schedules change it systematically. Warm up at the start to find a good region, then decay as training converges. The schedule significantly affects final performance and stability. Using a constant learning rate typically produces worse results than a well-tuned schedule." },
      { difficulty: "Easy", tag: "Fact", stopTitle: '', question: `Dropout randomly sets a fraction of neurons to zero during training to prevent overfitting.`, answer: true, verdict: "Correct.", explanation: "By randomly disabling neurons during training, dropout prevents co-adaptation and forces redundant representations. It's not used during inference — only training. It acts as ensemble learning: training many different subnetworks simultaneously." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `Layer normalization stabilizes training by standardizing activations, allowing higher learning rates and more stable deep network training.`, answer: true, verdict: "Correct.", explanation: "Without normalization, the distribution of activations changes as weights change — early layers keep shifting what later layers see. Layer normalization standardizes activations within each example, stabilizing this distribution and allowing higher learning rates." },
      { difficulty: "Medium", tag: "Hot take", stopTitle: '', question: `More training data always produces better models regardless of data quality.`, answer: false, verdict: "Wrong.", explanation: "Recent research has complicated this. Training on filtered, high-quality data often outperforms more lower-quality data. The Chinchilla scaling laws showed most large models were undertrained on too little data relative to size. Data curation is one of the most important and least discussed aspects of training." },
      { difficulty: "Medium", tag: "Example", stopTitle: '', question: `Distributed training splits large model training across many processors using techniques like model parallelism and data parallelism.`, answer: true, verdict: "Correct.", explanation: "No single processor can hold a 100-billion parameter model. Model parallelism splits the model across devices. Data parallelism runs copies on different devices with gradient synchronization. Modern training uses combinations of both plus pipeline parallelism." },
      { difficulty: "Hard", tag: "Scenario", stopTitle: '', question: `Fine-tuning and RLHF after pretraining are necessary because pretraining alone produces a model that predicts text but doesn't necessarily follow instructions helpfully.`, answer: true, verdict: "Correct.", explanation: "Pretraining on text prediction produces broad capabilities but not helpful behavior. Supervised fine-tuning on desired behavior examples, followed by reinforcement learning from human feedback, shapes how those capabilities are applied. ChatGPT and Claude are not just pretrained models." },
      { difficulty: "Hard", tag: "Myth bust", stopTitle: '', question: `Training large AI models is purely a scientific endeavor where known techniques are applied systematically.`, answer: false, verdict: "Wrong.", explanation: "Training at scale is simultaneously scientific, engineering, and empirical art. Every component matters. Mistakes in any of them can make training unstable or produce poor results. Researchers discover what works through careful experimentation at enormous scale. Many findings are empirical without complete theoretical understanding." },
    ],
  },
}

export default w6

# QA run log — 2026-08-01 "100% pass" (predictions first, then results)

Doctrine rule 4.2: predictions are written BEFORE each phase runs. Surprises
are the interesting rows. Zero surprises across the whole session would itself
be a red flag (tests not probing anything new).

## Phase B — mutation validation of the new harnesses (broken worktree)

Six deliberate breaks planted; every detector must fire or it gets fixed
before its green is ever believed.

| Mutation | Detector that must fire | Prediction | Result |
|---|---|---|---|
| M1 remove guard from /elementary/world/[id] | sweep wrong-track redirect check; monkey I2 | fires | FIRED (all 6 wrong personas flagged) |
| M2 hardcode "MODULES" onto middle-pt page | sweep EN-chrome-on-PT check | fires | FIRED |
| M3 render `undefined · NaN` in about page | sweep dev-artifact check | fires | FIRED (undefined AND NaN, 7 personas) |
| M4 console.error on home page mount | sweep console check; monkey I1 | fires | FIRED in both harnesses |
| M5 remove games-layout Exit button | sweep game-exit check | fires | FIRED (84 game loads flagged) |
| M6 /lessons renders null | sweep blank-page check | fires | FIRED |

### What the mutation phase caught — in the HARNESSES (the point of the phase)

1. **I truncated my own evidence**: piped the first mutant run through
   `tail -20` and briefly concluded M1/M3 detectors were blind. They weren't
   — my reporting was. Rule added: harness output always goes to a file,
   never through a truncating pipe.
2. **Sweep route inventory over-counted**: it trusted the id-range COMMENTS
   in elementary.ts, which lie (world 103 has 2 lessons, not 8). Now parses
   actual lesson entries from the data files. "Generated, not remembered"
   has to mean generated from evidence, not from someone's comment.
3. **Monkey was deaf to console errors** (only listened for page errors) —
   M4 sailed past it. Fixed; refired; caught.
4. **Monkey's off-origin recovery** could strand itself on about:blank
   (false I5). Fixed to re-enter the app by URL.
5. **Transient wrong-track false alarms** under dev-server load (guards
   hydrate late during cold compiles): three lessons flagged, all clean on
   individual re-probe. Sweep now rechecks once after +2.5s before flagging;
   Phase C runs against a production build where the noise source doesn't
   exist.
6. **Harness init-scripts threw SecurityErrors** in transitional
   about:blank documents, polluting I1. Guarded.

Surprise count for Phase B: 6 — all in the harnesses, none in the app.
Exactly why greens from unvalidated harnesses were never to be trusted.

## Phase C — full local matrix (fixed code, production build, port 3200)

Predictions:
- sweep (FULL matrix, ~1300 loads): 2–6 genuine findings. Most likely:
  console noise from video autoplay in headless; a page class I haven't
  audited (complete/*, handbook) leaking wrong-language chrome; possibly a
  blank page for an edge persona. I do NOT expect wrong-track redirect
  failures (guards are fresh and flow-tested).
- monkey (4 personas × 120 steps, seed 42): 0–3 violations. Most likely I1
  (console/page errors inside specific games) or I3 (dead end inside a
  game's end screen if any game hides the layout Exit via z-index).
- journey full matrix: clean except step 11 locally (no Groq key — expected,
  documented).
- flows: 39/39 plus new scenarios; I predict the two-tab scenario passes
  (storage events are shared) and chat-down shows SOME feedback — moderate
  confidence only, I haven't read PaiChatPanel's error path.
- content/lint/routes/viewports/games/build: all pass (ran this morning).

Results:
- sweep FULL matrix: **1,267 page loads, 7 personas, ZERO failures.**
  Prediction (2–6 findings) was WRONG — recorded as a surprise. The reason
  this green is believable anyway: this exact sweep was seen firing on all
  six mutations an hour earlier. Greens are trusted only because reds were
  witnessed first.
- monkey: seeds 42 + 777, six personas, 720 total steps — every violation
  was harness noise (Chromium logs our auth API's semantic 404s as resource
  errors). Filter fixed (allow 4xx only from /api/*), clean rerun. Zero app
  defects. Prediction (0–3) technically right but for the wrong reason —
  all findings were in the harness again.
- flows: 48/48 including new S9 (two tabs + remote sign-out), S10 (refresh
  mid-quiz), S11 (chat backend down → polite PT error, no crash).
- journey: 36 journeys (3 engines × 6 sizes × EN+PT) — steps 1–10 pass on
  ALL runs including previously-flaky webkit 768 and chromium 280. Step 11
  fails ×18 locally as documented (no Groq key exists off-Vercel).
- routes 136/136 · viewports pass · games 28/28 · lint clean · content
  parity clean · production build clean.

Phase C surprise count: 1 (sweep found nothing — my prediction was
miscalibrated toward pessimism; the morning's guard/language work covered
the matrix better than I gave it credit for).

## Phase D — production + fresh-context adversary

Predictions:
- prod sweep + flows + journey (chromium EN+PT): green.
- pressure test: blocked early by daily cap from this IP (already spent
  today); will run a reduced set and note the budget.
- adversary agent: I predict it finds at least one thing I missed or
  overclaimed — if it finds nothing at all, treat with suspicion and read
  its transcript for effort level.

### Results

Prod before adversary: full sweep 1,267 loads — one transient cold-start
flag, clean on double re-probe (sweep now rechecks 3×); flows 48/48;
journey 11/11 EN and PT; monkey 120 steps clean.

**Adversary (fresh context, claims only): BROKE 4 of 12 claims and found 6
unlisted defects.** The prediction held, emphatically. Findings and outcomes:

| Finding | Outcome |
|---|---|
| C1a untranslated SCENARIO tag (my own 1-day-old bug) | fixed + lint rule for unmapped tags |
| C1b /about fully English through hydration (~10s throttled) | fixed: blank until language known |
| C3/C11 /games + all 28 games unguarded, anonymous, English | fixed: signed-in required, headings localized |
| C5 transient auth error DESTROYS the session cookie | fixed: only definitive answers sign out |
| C9a English-only 404 with no way home | fixed: bilingual + home link |
| C9b PT onboarding errors shown in English (drifted keys) | fixed + PT strings |
| Other#1 **149/160 PT middle quiz explanations = question stems** | fixed: translated from corrected EN; lint hard-fails the signature |
| Other#3 username silently rewritten (João→joo) | fixed: live sanitized preview |
| Other#5 middle-pt home missing greeting | fixed |
| C10 bare "Sem resposta." on 5xx | fixed: polite unreachable message |
| Other#2 fix-the-robot "wrong replacement" | **falsified**: strikethrough correction; the adversary's text-only extraction can't see styling — adversaries have blind spots too |
| Other#8/#9 tap targets, small-height scrolling | pre-existing, tracked, not shipped-blocking |
| Other#7 route naming inconsistency | documented (post-launch refactor candidate) |

Why no in-house net caught these: the sweep only sees slide 1 of each lesson
(SCENARIO was on slide 5) and never submits quiz answers (explanations);
games were POLICY-allowed to everyone in the harness because they were
policy-allowed in the code — the harness inherited the app's assumption.
An independent context with no inherited assumptions is not optional.

Prod after fixes: flows 48/48, trimmed sweep 546 loads clean, direct probes
of all three headline reproductions (312 explanation, 311 CENÁRIO, PT login
error) pass on paiforkids.com.

Account cleanup owed on prod DB: zz_qa_*, zz_adv_*, zz_sweep_bot,
zz_monkey_bot, plus three the sanitizer stripped: `scriptalert1`,
`zzadvsp3126`, `bzz_adv_bold3126b`.

Phase D surprise count: 13 findings from the adversary, 1 of them false.
That ratio — against a system that had just passed 1,267-load sweeps and
36 clean journeys — is the whole argument for the fresh-context pass.
